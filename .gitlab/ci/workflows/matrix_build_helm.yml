.matrix_build_helm:
  parallel:
    matrix:
      - HELM_CHART_NAME: auth
        HELM_CHART_PATH: boundaries/auth/ops/$HELM_CHART_NAME

      - HELM_CHART_NAME: auth-ui
        HELM_CHART_PATH: boundaries/ui/ops/$HELM_CHART_NAME
